# personal shell config
export EDITOR=vim
export PS1='\h:\W $ '
alias ll='ls -lah'
alias ls='ls -G'
# tweak alias
# minor shell cleanup
# touch backup script
# adjust vim setting
# small git alias tweak
# fix typo in comment
# adjust vim setting
# minor shell cleanup
# cleanup whitespace
# readme wording
